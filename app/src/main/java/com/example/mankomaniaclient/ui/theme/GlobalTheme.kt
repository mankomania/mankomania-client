package com.example.mankomaniaclient.ui.theme

import androidx.compose.runtime.Composable

/**
 * # GlobalTheme
 *
 * Definiert das globale Design (Farben, Typografie, etc.) der App.
 *
 * @author
 * @since
 * @description Composable-Funktion, die MaterialTheme mit individuellen Werten versieht.
 */
@Composable
fun GlobalTheme(content: @Composable () -> Unit) {
    // TODO: Hier könnte MaterialTheme(...) aufgerufen werden
    content()
}